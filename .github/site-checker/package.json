{
  "private": true,
  "description": "script to check sites if webring sites are reachable",
  "scripts": {
    "check-sites": "node check.js"
  },
  "devDependencies": {
    "axios": "^0.25.0",
    "jsdom": "^19"
  }
}
